/**
 * @File rsa-set.js
 *
 * Handles the set of RSA keys used to communicate with the Minecraft map 
 * generator.
 */

class RSASet {
    constructor(publicKeyFile, privateKeyFile, remotePublicKeyFile) {
    }
}

module.exports = RSASet;
