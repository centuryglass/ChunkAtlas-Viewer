/**
 * @file staged-tile_writer.js
 *
 * Sets map tile image resource URIs of staged map tile entries. This should
 * be done for each staged tile when its corresponding image is uploaded.
 */

module.exports = {
};
